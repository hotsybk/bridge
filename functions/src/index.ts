// Cloud Functions exports — populated in Phase 1.3+
//
// Add each function as a named re-export so Firebase CLI can detect them, e.g.:
//   export { onUserCreated } from "./triggers/on-user-created";
//   export { subscriptionRunner } from "./scheduled/subscription-runner";
//   export { createOrder } from "./callable/create-order";

export {};
